import { useContext } from 'react';
import {{pascalCase name}} from "../context"

const use{{pascalCase name}} = () => {
  return useContext({{pascalCase name}});
};

export default use{{pascalCase name}};
